# 🗺️ 旅遊規劃專案開發進度表

這是我們穩紮穩打、一步一腳印的開發藍圖。每完成一個細項，我們就會在方框打勾 `[x]`。

---

## 📅 階段一：前置作業與環境對接 ✅ [100% 全數通關]
- [x] 1-1. 在 GitHub 建立 Remote Repository 並完成首次本機程式碼 Push 備份。
- [x] 1-2. 在本機專案根目錄設定 `.env.local`，正確填入 Supabase URL 與 Anon Key 程式碼環境變數。
- [x] 1-3. 建立前端 `src/lib/supabaseClient.ts` 初始化連線實例程式碼。

## 🗄️ 階段二：資料庫建置 (Supabase Backend) ✅ [100% 全數通關]
- [x] 2-1. 前往 Supabase SQL Editor 執行初始化資料表 Schema 腳本（建立景點與行程表）。
- [x] 2-2. 設定 Row Level Security (RLS) 安全防護原則，確保使用者程式碼只能讀寫自己的資料。
- [x] 2-3. 在本機執行 Supabase CLI 指令，自動生成對應資料庫的 TypeScript 型別定義檔案。

## 🔐 階段三：身分驗證系統 (Auth) ⏳ [目前進行中]
- [x] 3-1. 在專案中編寫 `src/middleware.ts` 伺服器端路由守衛程式碼。
- [x] 3-2. 確保 Supabase 控制台 Email Auth 基礎功能啟用（並暫時停用 Google OAuth 以簡化流程）。
- [ ] 3-3. 編寫前端 `/login` 頁面，實作「純帳號密碼註冊/登入」的 React Client UI 組件與核心邏輯（此時不包含 Google 按鈕）。

## 🧠 階段 3.5：【核心功能與 UI 風格專題討論】 🎯 [大腦激盪點]
> 💡 *注意：此階段不寫程式碼，專注於規格與畫面設計討論，收斂想法後才進入階段四。*
- [ ] 3.5-1. **確立 UI 組件庫**：正式導入 `shadcn/ui`，並確立網站的色彩基調（例如：極簡科技風、溫暖旅遊風）。
- [ ] 3.5-2. **拆解行程規劃器功能**：討論高度互動畫布的細項（天數切換、景點排序方式、多國語言欄位呈現、是否引入地圖連動）。
- [ ] 3.5-3. **定義公開行程邏輯**：確立使用者如何「一鍵公開行程」以及首頁看板的篩選機制。

## 🏗️ 階段四：核心功能開發 (行程與景點管理)
- [ ] 4-1. 編寫 `src/app/page.tsx` 使用 Server Component (SSR) 撈取並渲染公開旅遊行程。
- [ ] 4-2. 建立 `/dashboard` 頁面，使用 `"use client"` (SPA 模式) 編寫高度互動的行程規劃器（依據 3.5 討論結果實作）。
- [ ] 4-3. 實作新增、編輯、刪除景點的 API 呼叫程式碼（含中英韓多國語言欄位與 JSONB 多網址陣列處理）。

## 🚀 階段五：生產環境部署與功能延伸 (Deployment & OAuth)
- [ ] 5-1. 前往 Google Cloud Console 申請憑證，於 Supabase 啟用，並回頭在 `/login` 頁面「切出並實作」Google 第三方快速登入按鈕。
- [ ] 5-2. 在本機執行 `npm run build`，檢查 TypeScript 型別與編譯程式碼是否完全無過失。
- [ ] 5-3. 匯入 Vercel 平台進行自動化 CI/CD 部署並補全環境變數，正式上線。