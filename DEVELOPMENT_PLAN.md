# 🗺️ 旅遊規劃專案開發進度表

這是我們穩紮穩打、一步一腳印的開發藍圖。每完成一個細項，我們就會在方框打勾 `[x]`。

---

## 📅 階段一：前置作業與環境對接 ✅ [100% 全數通關]
- [x] 1-1. 在 GitHub 建立 Remote Repository 並完成首次本機程式碼 Push 備份。
- [x] 1-2. 在本機專案根目錄設定 `.env.local`，正確填入 Supabase URL 與 Anon Key 程式碼環境變數。
- [x] 1-3. 建立前端 `src/lib/supabaseClient.ts` 初始化連線實例程式碼。

## 🗄️ 階段二：資料庫建置 (Supabase Backend) ✅ [100% 全數通關]
- [x] 2-1. 前往 Supabase SQL Editor 執行初始化資料表 Schema 腳本（建立景點與行程表）。
- [x] 2-2. 設定 Row Level Security (RLS) 安全防護原則，確保使用者程式碼只能讀寫自己的資料。
- [x] 2-3. 在本機執行 Supabase CLI 指令，自動生成對應資料庫的 TypeScript 型別定義檔案。

## 🔐 階段三：身分驗證系統 (Auth) ✅
- [x] 3-1. 在專案中編寫 `src/middleware.ts` 伺服器端路由守衛程式碼。
- [x] 3-2. 確保 Supabase 控制台 Email Auth 基礎功能啟用（並暫時停用 Google OAuth 以簡化流程）。
- [x] 3-3. 編寫前端 `/login` 頁面，實作「純帳號密碼註冊/登入」的 React Client UI 組件與核心邏輯，並處理好 `useEffect` 客戶端雙重安全攔截（消除 ESLint Promise 警告）。

## 🧠 階段 3.5：【核心功能與 UI 風格專題討論】 🎯 [大腦激盪點]
> 💡 *注意：此階段不寫程式碼，專注於規格與畫面設計討論，收斂想法後才進入階段四。*
- [ ] 3.5-1. **確立 UI 組件庫**：正式導入 `shadcn/ui`，並確立網站的色彩基調（例如：極簡科技風、溫暖旅遊風）。
- [ ] 3.5-2. **拆解行程規劃器功能**：討論高度互動畫布的細項（天數切換、景點拖曳排序、多國語言欄位呈現、是否引入地圖連動）。
- [ ] 3.5-3. **定義公開行程邏輯**：確立使用者如何「一鍵公開行程」以及首頁看板的篩選機制。
- [ ] 3.5-4. **儲存與多媒體策略討論**：討論行程封面圖處理機制（使用者自訂上傳、Unsplash API 自動配圖、或固定預設圖，評估是否啟用 Supabase Storage）。

## 🏗️ 階段四：核心功能開發 (行程與景點管理)
- [ ] 4-1. **建立專案頁面路由藍圖**：正式在 `src/app` 下建立核心路由結構（首頁 `/`、`/login`、總覽 `/trips`、動態詳細頁兼編輯器 `/trips/[id]`）。
- [ ] 4-2. 編寫 `src/app/page.tsx` 使用 Server Component (SSR) 撈取並渲染公開旅遊行程（首頁形象看板）。
- [ ] 4-3. 實作 `/trips` 個人行程總覽大廳，以網格卡片（Grid Cards）呈現該登入使用者擁有的所有行程（並在此實作 AI 新增行程彈出視窗）。
- [ ] 4-4. 建立高度互動的 `/trips/[id]` 動態保護頁面，實作「所見即所得」行程編輯器，判定擁有者時啟用 CRUD，非擁有者時切換為唯讀分享模式。
- [ ] 4-5. **實作景點順序調整與時間計算**：實作景點排序（Sequence/Order），並處理跨國旅遊的時區轉換與交通時間備忘欄位。
- [ ] 4-6. 實作 API 呼叫程式碼，妥善處理中英韓多國語言欄位與 JSONB 多網址（如參考遊記、交通票券連結）陣列。
- [ ] 4-7. **實作「一鍵公開/私密」切換**：在前端加入 Toggle 切換開關，同步更新資料庫 `is_public` 欄位。

## 🚀 階段五：生產環境部署與功能延伸 (Deployment & OAuth)
- [ ] 5-1. 前往 Google Cloud Console 申請憑證，於 Supabase 啟用，並回頭在 `/login` 頁面「切出並實作」Google 第三方快速登入按鈕。
- [ ] 5-2. **串接 Resend 免費 SMTP**：免買網域，直接串接 Resend 免費測試額度，解鎖並中文化 Supabase 驗證信（打造兩人專屬溫馨開場）。
- [ ] 5-3. **生產環境安全性審查 (Security Audit)**：全面檢查 Supabase RLS 防禦狀態，確保只有「我們兩人」的帳號能動到濟州島的私密行程。
- [ ] 5-4. 在本機執行 `npm run build`，檢查 TypeScript 型別與編譯程式碼是否完全無過失。
- [ ] 5-5. 匯入 Vercel 平台進行自動化部署，取得免費 `.vercel.app` 網址，正式上線發布。